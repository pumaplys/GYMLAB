{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "GYMLAB — libreria (packages/*)",
  "extends": "./base.json",
  "compilerOptions": {
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["node"]
  }
}
