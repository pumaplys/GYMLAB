# =============================================================
# GYMLAB — plantilla de variables de entorno
# Copia este archivo a .env y rellena los valores.
# .env esta en .gitignore: NUNCA se sube al repositorio.
# =============================================================

NODE_ENV=development

# --- Base de datos (PostgreSQL) --------------------------------
# Hay DOS conexiones a proposito, y no son intercambiables.
#
# Rol propietario: migraciones, seed y aplicacion de politicas.
# Es superusuario, asi que IGNORA Row Level Security.
# La API NUNCA debe usar esta.
DATABASE_URL=postgresql://gymlab:gymlab@localhost:5432/gymlab

# Rol de la aplicacion: sin privilegios especiales, sujeto a RLS.
# Es la que usan la API en ejecucion y los tests de aislamiento.
DATABASE_URL_APP=postgresql://gymlab_app:gymlab_app@localhost:5432/gymlab

# Contrasena con la que `pnpm db:migrate` crea/actualiza el rol gymlab_app.
# Debe coincidir con la de DATABASE_URL_APP. En produccion, un secreto real.
APP_DB_PASSWORD=gymlab_app

# --- API -------------------------------------------------------
API_PORT=3001
API_URL=http://localhost:3001

# Secreto de firma de Better Auth. Minimo 32 caracteres.
# En produccion: un secreto real, distinto por entorno y rotable.
AUTH_SECRET=desarrollo-cambia-esto-por-un-secreto-de-verdad

# Codigo que exige /v1/auth/register-gym mientras estemos en piloto.
# Se sustituira por el flujo de pago cuando entre Stripe.
PLATFORM_INVITE_CODE=gymlab-piloto-2026

# Origenes permitidos por CORS, separados por comas.
# Lista blanca y nunca `*`: el transporte por cookie exige `credentials: true`,
# y el navegador rechaza esa combinacion con comodin.
#
# En produccion no llega a usarse —el panel se sirve desde la propia API, asi que
# no hay peticion entre origenes— pero el compose la declara igualmente con el
# dominio publico: si se dejara entrar este valor por defecto, `localhost:3000`
# quedaria como origen de confianza CON credenciales en un despliegue publico.
# La API se niega a arrancar en produccion si aqui hay un origen local o HTTP.
#
# Aqui hace falta porque `next dev` corre en el 3000.
CORS_ORIGINS=http://localhost:3000

# Cuantos proxies de confianza hay DELANTE del proceso.
#
# NO ES COSMETICO: de esto depende el limite de intentos de login, la IP que se
# guarda en auditoria y la de los consentimientos.
#
#   0  sin proxy delante (desarrollo, o el contenedor expuesto directamente)
#   1  detras de Caddy, Nginx o de una plataforma gestionada
#
# Medido en contenedor con Caddy delante: a 0 la IP de TODO EL MUNDO pasa a ser
# la del proxy, asi que el gimnasio entero comparte cubo de intentos. Y lo que
# de verdad permite falsear la IP no es este numero, sino poder alcanzar el
# puerto de la API sin pasar por el proxy — por eso el contenedor lo publica
# solo en el bucle local. El detalle completo, en docs/14-hardening-preproduccion.md.
TRUST_PROXY=0

# Carpeta del panel ya exportado. Si existe, la API lo sirve BAJO EL MISMO
# ORIGEN, que es el requisito de la sesion por cookie (ver docs/06-despliegue.md).
#
# En desarrollo se deja sin valor: el panel corre en `next dev` y esta carpeta no
# existe, asi que la API arranca solo como API. La imagen de produccion la fija.
# WEB_DIST_PATH=/app/web

# --- Correo ----------------------------------------------------------
# URL del panel web. Es la BASE DE LOS ENLACES DE LOS CORREOS: invitacion,
# restablecer contrasena y verificar email apuntan aqui, no a la API.
WEB_APP_URL=http://localhost:3000

# Clave de Resend. OPCIONAL en desarrollo: sin ella, los correos se registran
# en consola y se puede recorrer el flujo completo sin cuenta de Resend.
# En PRODUCCION es obligatoria — el proceso no arranca sin ella.
# RESEND_API_KEY=re_...

# Remitente. Debe ser un dominio verificado en Resend: enviar desde un dominio
# sin verificar acaba en spam, que es peor que no enviar (nadie se entera).
EMAIL_FROM=GYMLAB <no-reply@localhost>

# --- Panel web -------------------------------------------------
# OPCIONAL, y lo normal es no ponerla.
#
# Sin valor, el panel llama a `/v1` en su propio origen, que es el requisito de
# despliegue: frontend y API bajo el mismo dominio, para que la cookie de sesion
# sea de primera parte. En desarrollo tampoco hace falta — el panel ya apunta al
# 3001 cuando `NODE_ENV=development`.
#
# Solo sirve para un entorno donde la API viva en otro origen (una preview, por
# ejemplo). Se incrusta al construir: cambiarla exige volver a construir.
# NEXT_PUBLIC_API_URL=https://api.ejemplo.test

# --- App movil (Expo) ------------------------------------------
# En dispositivo fisico usa la IP de tu equipo, no localhost.
EXPO_PUBLIC_API_URL=http://localhost:3001

# Semilla de la que se derivan las claves de firma del QR de acceso (HKDF por gimnasio).
ACCESS_TOKEN_SECRET=desarrollo-semilla-de-acceso-cambiala-en-produccion

# Version vigente del consentimiento de datos de salud (RGPD art. 9).
# Sin valor, el modulo de progreso BLOQUEA todo registro: falta el texto legal.
# HEALTH_CONSENT_VERSION=2026-09-01

# --- Solo para el despliegue en VPS ----------------------------
# Estas NO se usan en desarrollo. Las lee docker/compose.produccion.yml, que
# vive en `docker/`: acuerdate de `--env-file .env` o Compose no las encuentra
# y las sustituye por cadenas vacias.
#
# DOMINIO alimenta a la vez API_URL, WEB_APP_URL y CORS_ORIGINS. Tiene que ser
# la URL publica CON https://, porque de ella deduce Better Auth si la cookie de
# sesion lleva `Secure`. Si falta, la API se niega a arrancar en produccion en
# lugar de degradar la sesion en silencio.
# DOMINIO=https://gimnasio.tudominio.com
# POSTGRES_PASSWORD=
# APP_DB_PASSWORD=
