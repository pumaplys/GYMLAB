/**
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │ LA VERSION NATIVA: NO HAY VISTA PREVIA. SON LAS LLAMADAS DE VERDAD.     │
 * │                                                                          │
 * │ Metro elige `fuente.web.ts` al compilar para web y ESTE fichero para iOS │
 * │ y Android. Ni un dato de muestra viaja en el binario.                    │
 * └──────────────────────────────────────────────────────────────────────────┘
 */
export {
  borrarCuentaReal as borrarCuenta,
  consultarBorradoReal as consultarBorrado,
} from './fuente-real';
