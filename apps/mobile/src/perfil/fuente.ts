/**
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │ LA VERSION NATIVA: NO HAY VISTA PREVIA. SON LAS LLAMADAS DE VERDAD.     │
 * │                                                                          │
 * │ Metro elige `fuente.web.ts` al compilar para web y ESTE fichero para iOS │
 * │ y Android. Ningun dato de muestra se empaqueta.                          │
 * └──────────────────────────────────────────────────────────────────────────┘
 */
export {
  aceptarPrivacidadReal as aceptarPrivacidad,
  cargarAccesosReal as cargarAccesos,
  cargarFichaReal as cargarFicha,
  cargarPagosReal as cargarPagos,
  cargarPrivacidadReal as cargarPrivacidad,
  retirarPrivacidadReal as retirarPrivacidad,
} from './fuente-real';
