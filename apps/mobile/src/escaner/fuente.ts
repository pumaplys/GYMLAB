/**
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │ LA VERSION NATIVA: NO HAY VISTA PREVIA. ES LA LLAMADA DE VERDAD.        │
 * │                                                                          │
 * │ Metro elige `fuente.web.ts` al compilar para web y ESTE fichero para iOS │
 * │ y Android. Ningun veredicto de muestra se empaqueta en el binario.       │
 * └──────────────────────────────────────────────────────────────────────────┘
 */
export { verificarCarneReal as verificarCarne } from './fuente-real';
