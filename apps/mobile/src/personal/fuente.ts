/**
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │ LA VERSION NATIVA: NO HAY VISTA PREVIA. SON LAS LLAMADAS DE VERDAD.     │
 * │                                                                          │
 * │ Metro elige `fuente.web.ts` al compilar para web y ESTE fichero para iOS │
 * │ y Android. Ni un nombre de muestra viaja en el binario.                  │
 * └──────────────────────────────────────────────────────────────────────────┘
 */
export {
  asignarEntrenadorReal as asignarEntrenador,
  cargarAccesosReal as cargarAccesos,
  cargarEntrenadoresDeSocioReal as cargarEntrenadoresDeSocio,
  cargarEntrenadoresReal as cargarEntrenadores,
  cargarInvitacionesReal as cargarInvitaciones,
  cargarPersonalReal as cargarPersonal,
  crearInvitacionReal as crearInvitacion,
  retirarAccesoReal as retirarAcceso,
  retirarEntrenadorReal as retirarEntrenador,
  revocarInvitacionReal as revocarInvitacion,
} from './fuente-real';
